# 闲置微铺（Idle）

> 一个轻量级的本地 / 校园二手闲置交易小平台：买家发布闲置、他人担保购买、平台托管资金、后台确认发货后结算。前后端一体，原生 HTML/CSS/JS + PHP + MySQL，零构建步骤，开箱即用。

---

## 一、项目简介

闲置微铺（代号 `xzwp`，英文名 **Idle**）是一个面向小范围（校园 / 社区 / 兴趣小组）的 C2C 二手交易平台，核心特色是**担保交易**：

- 买家下单后，款项先由「平台」托管（冻结在买家账户，不立即打给卖家）；
- 卖家在后台「订单管理」点击「确认发货」后，托管资金才结算给卖家；
- 若订单被「驳回」，款项原路退回买家，商品自动重新上架。

平台不碰真实资金通道，所有余额均在系统内部记账，适合作为**演示 / 学习 / 内部小范围使用**。

---

## 二、功能特性

| 模块 | 功能 |
| --- | --- |
| 用户 | 注册 / 登录 / 退出、修改资料、设置头像、余额查询 |
| 商品 | 发布闲置、上传图片、编辑、下架、我的商品管理 |
| 交易 | 担保购买（平台托管 → 确认发货结算 / 驳回退款） |
| 订单 | 买家端「我的订单」按状态筛选（待发货 / 已完成 / 已驳回） |
| 资金 | 余额充值（后台审批入账）、余额提现（后台审批出金） |
| 后台 | 仪表盘、用户管理、商品管理、订单管理、财务审核、支付配置 |

---

## 三、技术栈

- **前端**：原生 HTML / CSS / JavaScript + [Vue 3](https://v3.vuejs.org/)（Options API，本地引入 `vue.global.prod.js`，无打包构建）
- **后端**：PHP（PDO + MySQL），REST 风格接口（`api/*.php`）
- **后台**：独立的 `administerint/` 目录，与前端双会话隔离（前端 `uid` / 后台 `admin_id`）
- **存储**：MySQL（建表脚本 `api/install.sql`）
- **运行**：任意支持 PHP + MySQL 的 Web 服务器（Nginx / Apache）

---

## 四、目录结构

```
xzwp/
├── index.html              # 站点首页（引导 / 入口）
├── page/                   # 前端页面
│   ├── index.html          # 首页（商品列表、购买弹窗）
│   ├── login.html          # 登录 / 注册
│   ├── user.html           # 个人中心（余额、头像、提现）
│   ├── manage.html         # 商品管理（发布 / 编辑 / 下架）
│   ├── orders.html         # 我的订单（状态筛选）
│   ├── recharge.html       # 充值 / 提现
│   ├── js/                 # 前端逻辑（Vue 实例）
│   └── css/                # 前端样式
├── administerint/          # 后台管理（独立会话）
│   ├── admin.html          # 后台登录
│   ├── dashboard.html      # 仪表盘
│   ├── users.html          # 用户管理
│   ├── products.html       # 商品管理
│   ├── orders.html         # 订单管理（担保交易结算）
│   ├── finance.html        # 财务审核（充值 / 提现）
│   ├── payconfig.html      # 支付配置
│   ├── js/                 # 后台逻辑
│   └── css/                # 后台样式
├── api/                    # 后端接口
│   ├── config.php          # 数据库配置（需自行填写）
│   ├── auth.php            # 注册 / 登录 / 会话 / 个人信息
│   ├── products.php        # 商品增删改查
│   ├── order.php           # 下单（托管）/ 我的订单
│   ├── wallet.php          # 充值 / 提现
│   ├── admin.php           # 后台：用户 / 商品 / 订单结算 / 财务
│   ├── finance.php         # 财务明细
│   ├── captcha.php         # 图形验证码
│   ├── setup.php           # 一键建表 / 补索引
│   ├── install.sql         # 数据库初始化 SQL
│   └── uploads/            # 用户上传文件（商品图 / 头像）
└── js/v3.2.8/              # 本地引入的 Vue 3 运行库
```

---

## 五、安装与部署

1. **准备环境**：PHP 8.x + MySQL 5.7+，并启用 `pdo_mysql` 扩展。
2. **导入数据库**：在 MySQL 中创建库（如 `xzwp`），执行 `api/install.sql` 建表；或部署后访问 `api/setup.php` 自动初始化。
3. **配置数据库**：编辑 `api/config.php`，填写数据库主机、库名、用户名、密码。
4. **配置上传目录**：确保 `api/uploads/` 可写（Web 服务器用户有写权限）。
5. **部署站点**：将整个目录放到 Web 根目录（如宝塔建站指向 `xzwp/`），确保 `api/` 可通过 HTTP 访问。
6. **访问**：
   - 前端首页：`http(s)://你的域名/index.html`
   - 后台入口：`http(s)://你的域名/administerint/admin.html`

> 默认后台账号请在部署后于 `users` 表手动插入管理员，或通过后台注册首个账号后于数据库将其标记为管理员（具体字段见 `install.sql`）。

---

## 六、核心业务流程（担保交易）

```
买家浏览商品
   │
   ▼
点击「购买」→ 弹窗确认
   │
   ▼
后端下单：扣减买家余额，订单状态 = pending（平台托管），商品下架
   │
   ▼
后台「订单管理」：
   ├── 确认发货（ship）  → 托管资金打款给发布人，订单 completed
   └── 驳回（reject）    → 退款退回买家余额，商品重新上架，订单 rejected
   │
   ▼
买家「我的订单」可见：待发货 / 已完成 / 已驳回
```

**资金安全要点**

- 下单时**只扣买家、不立即打款**，资金处于「平台托管」状态，规避跑单风险。
- 充值采用**审批制**：用户提交充值后仅生成 `pending` 记录，需后台在「财务审核」中确认入账，余额才会增加。
- 提现为**申请制**：用户提交提现后冻结对应余额，需后台审批通过后实际出金（本项目为内部记账演示，未对接真实支付通道）。

---

## 七、前端使用说明

| 页面 | 操作 |
| --- | --- |
| 首页 | 浏览商品、点击图片可放大保存、点击「购买」进入担保流程 |
| 登录 / 注册 | 图形验证码校验后注册或登录 |
| 个人中心 | 查看余额与头像（每次打开实时拉取服务端）、提现 |
| 商品管理 | 发布闲置（图片 120×120 正方形）、编辑、下架 |
| 我的订单 | 按「全部 / 待发货 / 已完成 / 已驳回」筛选查看 |
| 充值 / 提现 | 提交充值 / 提现申请，等待后台审核 |

## 八、后台管理说明

| 模块 | 操作 |
| --- | --- |
| 仪表盘 | 查看运营总览数据 |
| 用户管理 | 查看 / 禁用 / 删除用户 |
| 商品管理 | 查看 / 下架 / 删除违规商品 |
| 订单管理 | **担保交易结算**：确认发货（打款）/ 驳回（退款） |
| 财务审核 | 充值审批入账、提现审批出金 |
| 支付配置 | 配置平台收款信息 |

---

## 九、开源声明与免责

本项目以开源形式提供，相关**免责条款、责任限制与合规要求**请查阅 [**DISCLAIMER.md**](./DISCLAIMER.md)。

---

## 十、许可

本项目采用 MIT 许可证开源，详见 [LICENSE](./LICENSE) 文件。
