/* =========================================================
 * config.js — 闲置微铺 全局 API 地址配置（统一入口）
 * ---------------------------------------------------------
 * 修改后端服务器地址时，【只需改这一个文件】。
 * common.js / shared.js 会优先读取 window.XZWP_API；
 * 若本文件未被加载，则使用各自内部的兜底默认值。
 * 所有前端(page/)与后台(administerint/)页面都会在
 * common.js / shared.js 之前加载本文件。
 * ========================================================= */
window.XZWP_API = 'http://192.168.0.26/api/';
